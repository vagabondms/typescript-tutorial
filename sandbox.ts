// arrays
let names = ['luigi', 'mario', 'yoshi'];

names.push('toad');
// names.push(3);
// names[0] = 3;

let numbers = [10, 20, 30, 40];
numbers.push(25);
// numbers.push('minseok');
// numbers[1] = 'minseok';

let mixed = ['key', 4, 'hi', 6, 4];

// objects
let ninja = {
  name: 'mario',
  belt: 'black',
  age: 30,
};
